{% extends 'template.html.twig' %}

{% block title %}Liste des attributs additionnels rep{% endblock %}

{% block body %}
				<div class="page-inner mt--5">
					<div class="row mt--2">
						<div class="col-md-12">
							<div class="card">
								<div class="card-header">
									<div class="d-flex align-items-center">
										<h4 class="card-title">Liste des attributs additionnels rep</h4>
										<a href="{{ path('attribut_add_rep_new') }}" class="btn btn-primary btn-round ml-auto">
											<i class="fa fa-plus"></i>
											Ajouter un attributs additionnels rep
										</a>
									</div>
								</div>
								<div class="card-body">
									

									<div class="table-responsive">
										<table id="add-row" class="display table table-striped table-hover" >
        <thead>
            <tr>
                <th>Id</th>
                <th>Reponse</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for attribut_add_rep in attribut_add_reps %}
            <tr>
                <td>{{ attribut_add_rep.id }}</td>
                <td>{{ attribut_add_rep.reponse }}</td>
                <td>
                    <a href="{{ path('attribut_add_rep_show', {'id': attribut_add_rep.id}) }}" class="btn btn-link btn-primary btn-lg" ><i class="fa fa-eye"></i> show</a>
                    <a href="{{ path('attribut_add_rep_edit', {'id': attribut_add_rep.id}) }}" class="btn btn-link btn-danger" ><i class="fa fa-edit"></i> show</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
								</div>
							</div>
						</div> 	
						
					</div>
					
				</div>
{% endblock %}			