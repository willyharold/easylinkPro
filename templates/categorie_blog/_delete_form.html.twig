<form method="post" style="float:right;" action="{{ path('categorie_blog_delete', {'id': categorie_blog.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_blog.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash"></i> </button>
</form>
