<form method="post" action="{{ path('affectation_confirme_delete', {'id': affectation_confirme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ affectation_confirme.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash"></i> Delete</button>
</form>
